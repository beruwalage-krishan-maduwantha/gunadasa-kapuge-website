{
  "name": "gunadasa-kapuge-website",
  "version": "1.0.0",
  "private": true,
  "description": "Tribute website for Gunadasa Kapuge (1945-2003) — MERN stack, cinematic old-vibe design",
  "scripts": {
    "dev": "concurrently -n server,client -c yellow,cyan \"npm --prefix server run dev\" \"npm --prefix client run dev\"",
    "build": "npm --prefix client run build",
    "start": "npm --prefix server run start"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
